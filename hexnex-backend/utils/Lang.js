// hexnex-backend/utils/Lang.js

export const main = async (data, prompt) => {
  // For now, just return a dummy response
  return `✅ Dummy cybersecurity answer generated for: "${data}" using prompt: "${prompt}"`;
};
